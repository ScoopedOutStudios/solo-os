Use the Solo OS workflow in GitHub Projects/Issues.

Create the next Build Loop for this work:

<roadmap link, idea link, or direct objective>

Please:
1. Decide whether this should be a direct Build Loop or a child of an existing Roadmap item.
2. Create a bounded Build Loop with one primary goal and explicit non-goals.
3. Use the canonical Build Loop body template.
4. Set an honest risk tier.
5. Define the validation plan, release/rollback plan, and learning question.
6. Recommend the execution mode: `simple mode` by default, or `isolated mode` only if worktrees are clearly justified.
7. Recommend initial Status and whether any child tasks are actually necessary.
8. Create or update the GitHub issue using Solo OS commands.
